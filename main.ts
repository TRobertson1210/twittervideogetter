function main(){
    // I don't know, just paste this into a bookmarklet
    javascript:window.open(document.querySelectorAll('video').item(0).src);window.opener.location.reload(false);window.opener.location.replace();
    
}

